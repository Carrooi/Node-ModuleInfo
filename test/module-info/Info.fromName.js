// Generated by CoffeeScript 1.6.3
(function() {
  var Info, dir, expect, info, path;

  expect = require('chai').expect;

  path = require('path');

  Info = require('../../lib/Info');

  dir = path.normalize(__dirname + '/..');

  info = null;

  describe('Info.fromName', function() {
    it('should create Info object from module name', function() {
      info = Info.fromName(module, 'simple');
      return expect(info.getData()).to.be.eql({
        name: 'simple',
        main: './index.js'
      });
    });
    it('should create Info object for advanced module', function() {
      info = Info.fromName(module, 'advanced');
      return expect(info.getData()).to.be.eql({
        name: 'advanced',
        main: './dir/lib'
      });
    });
    return describe('#getMainFile()', function() {
      return it('should get main file for advanced module', function() {
        info = Info.fromName(module, 'advanced');
        return expect(info.getMainFile()).to.be.equal(dir + '/node_modules/advanced/dir/lib/index.js');
      });
    });
  });

}).call(this);
